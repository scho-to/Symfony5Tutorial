{% for category in main_categories %}
    <a class="p-2 text-dark" href="{{path('video_list', {'categoryname':category.name|lower, 'id':category.id})}}">{{category.name}}</a>
{% endfor %}
<a class="p-2 text-dark" href="{{path('admin_main_page')}}">My account</a>